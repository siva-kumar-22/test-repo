pipeline{
  agent any
  environment{
       wish = "good evening"
  }
  stages{
     stage("checkout"){
         script{
            def name= "name"
         echo "hi, ${name} , ${env.wish}"
        }
     }
 }
}
